<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.levware.common.mappers.repo.UserPwdChgMapper">

	<!-- 사용자 비밀번호 초기화 Result Map 작성자 박수연-->
	<resultMap type="UserInfoVO" id="UserInfoVOMap">
		<result property="userId" column="USER_ID" />
		<result property="authHint" column="AUTH_HINT" />
		<result property="authHintAns" column="AUTH_HINT_ANS" />
		<result property="userPassward" column="USER_PASSWORD" />
	</resultMap>
	
	<!-- 사용자 비밀번호 초기화 아이디 검색으로 비밀번호 힌트 조회-->
	<select id="getUserHintList" parameterType="String" resultMap="UserInfoVOMap">
		SELECT USER_ID, AUTH_HINT, AUTH_HINT_ANS, USER_PASSWORD
        FROM TB_OLAP_USR 
        WHERE USER_ID= #{userId}
	</select>

	<!-- 사용자 비밀번호 초기화 작성자 박수연 -->
	<update id="actionInitialize" parameterType="UserInfoVO">
		UPDATE TB_OLAP_USR 
		SET USER_PASSWORD = #{userPassward}
			,UPDATE_ID = #{userId}
        	,UPDATE_DT = to_char(SYSDATE,'yyyymmdd') 
        	,UPDATE_TM = to_char(SYSDATE,'hh24miss')
        WHERE USER_ID= #{userId}
	</update>
	
	<!-- 사용자 비밀번호 변경 작성자 박수연 -->
	<update id="actionPwdChange" parameterType="UserInfoVO">
		UPDATE TB_OLAP_USR 
		SET USER_PASSWORD = #{userPassward}
			,UPDATE_ID = #{userId}
        	,UPDATE_DT = to_char(SYSDATE,'yyyymmdd') 
        	,UPDATE_TM = to_char(SYSDATE,'hh24miss')
        WHERE USER_ID= #{userId}
	</update>

	
	
</mapper>